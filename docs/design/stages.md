######## S0 Contracts and foundations (contract freeze)
---- [core] paths: pom.xml, core/pom.xml, core/src/main/java/module-info.java, core/src/main/java/ru/cashprediction/core/ui/**, core/src/main/java/ru/cashprediction/core/app/**, core/src/main/java/ru/cashprediction/core/io/FolderListing.java, core/src/main/java/ru/cashprediction/core/session/MainWindowState.java, core/src/main/java/ru/cashprediction/core/session/WindowType.java, core/src/main/java/ru/cashprediction/core/session/codec/**, core/src/main/java/ru/cashprediction/core/session/store/RegistrySessionStore.java, core/src/main/resources/ru/cashprediction/core/ui/**, core/src/test/java/ru/cashprediction/core/ui/text/**, core/src/test/java/ru/cashprediction/core/ui/token/**, core/src/test/java/ru/cashprediction/core/app/fake/**, core/src/test/java/ru/cashprediction/core/app/env/**, core/src/test/java/ru/cashprediction/core/session/codec/**, docs/ui-spec.md, docs/ui-architecture.md, docs/ui-protocol.md
Freeze every public contract in design §3: records, sealed interfaces, UiPort, UiIntents, FormLogic, FormSession API, CommandId (complete list), MenuNode, ToolbarNode, view models, ChartScene, AlertSpec, UiDump records, and the flow class skeletons in core.app.flow, all with Russian Javadoc. Method bodies are stubs that throw UnsupportedOperationException. Also implement for real: the UiText loader with empty per-area properties files; DesignTokens, ColorToken, FontToken and TokenCss; UiFormats (whole, wholeSigned, compact, horizonLabel); SamplePlan in core.app; LaunchOptions, AppEnvironment and AppClock (arguments --home, --registry-node, --registry memory, --today, --ui, --selftest, --selftest-out, --selftest-recovery, --test-api); the registry node property plus a RegistrySessionStore.forClient(client, nodePrefix) overload, rejecting prefixes outside ru/cashprediction/; the additive MainWindowState.whatIfExtra field in all three codecs, keeping the old 8-argument constructor; WindowType ADJUSTMENT_EDITOR title changed to «Корректировка события»; FakeUiPort test fixture. Register the ui-parity module only inside the ui-tests and e2e profiles. Commit ui-spec-v2.md from the judge scratchpad verbatim as docs/ui-spec.md, write docs/ui-architecture.md from the design, and write docs/ui-protocol.md with the JSON shapes of design §5. Every new exported package contains a real class.
DONE: mvn -B install is green; the existing 499 tests still pass; the legacy ui-fx, ui-swing and web modules compile and run unchanged | UiFormatsTest covers every example in spec Форматы (compact «1,5 млн», «2 млн», «896 тыс», «950»; horizonLabel «до 31.08.2027») | RegistrySessionStoreNodeTest: the property and the overload are honoured, an invalid prefix is rejected, and the default node is unchanged | Codec tests: a snapshot without whatIfExtra decodes in XML, JSON and Markdown; round trip with whatIfExtra and with the pastExpanded, whatIfIncome and whatIfExpense filter keys; WindowType titles are not persisted | UiTextCatalogTest skeleton runs (duplicate keys, placeholder count, glyph set, no ISO date or Latin OK) | docs/ui-spec.md is byte-identical to the scratchpad ui-spec-v2.md; docs/ui-protocol.md lists every intent, query and effect type
---- [test-infra] paths: ui-parity/**
Create the skeleton of the test-only ui-parity module (JUnit only, no external libraries). ClientLauncher starts a separate JVM from reactor module jars with the design §6.3 argument list. ProcessTree kills a process and all its descendants through ProcessHandle. RegistryNodeCleaner deletes ru/cashprediction/selftest/<uuid> through java.util.prefs and records the modification stamps of the real session/fx and session/swing nodes. EdgeLauncher starts headless Edge. CdpClient uses java.net.http HttpClient and WebSocket and supports only Page.captureScreenshot and Runtime.evaluate. Launch argument names come from design §3.8 and must not be invented. Use a test-only dummy main class so this task does not depend on client changes.
DONE: mvn -B -Pui-tests -pl ui-parity -am verify runs LauncherSmokeIT: the dummy JVM is launched with an isolated home, then its process tree is killed and no child process survives | RegistryNodeCleanerIT creates and deletes a selftest node and proves the real session nodes were not modified | EdgeCdpSmokeIT loads a static test page in headless Edge and saves a 1200x800 PNG (skipped with a clear message if msedge is absent) | The default mvn -B install does not build ui-parity
######## S1 Core UI: texts, models, forms, alerts, legacy fixtures
---- [core-menu] paths: core/src/main/java/ru/cashprediction/core/ui/command/**, core/src/main/java/ru/cashprediction/core/ui/menu/**, core/src/main/java/ru/cashprediction/core/ui/view/status/**, core/src/main/resources/ru/cashprediction/core/ui/text/menu.properties, core/src/main/resources/ru/cashprediction/core/ui/text/toolbar.properties, core/src/main/resources/ru/cashprediction/core/ui/text/status.properties, core/src/main/resources/ru/cashprediction/core/ui/text/hotkeys.properties, core/src/test/java/ru/cashprediction/core/ui/command/**, core/src/test/java/ru/cashprediction/core/ui/menu/**, core/src/test/java/ru/cashprediction/core/ui/view/status/**
Implement CommandAvailability (with status.hint keys), HotkeyTable (desktop and web chords, scopes, text()), MenuModels.menuBar, toolbar and contextMenu for every target (row, total, past header, card, chart, preview), and StatusBuilder with all segments of spec §5.4, including session store texts and the persistent messages. All texts and tooltips of spec §3, §4, §5.1–5.4 context menus, §8.1 and §8.3 go into the owned properties files.
DONE: MenuModelsTest is table-driven from spec §3 and §4: every label, tooltip, accelerator (desktop and web), kind, order, group, enabled and checked state, with no adjacent separators | ContextMenuTest covers every row kind, total, past header, card with and without a date, chart inside and outside the plot area, and the preview list | CommandAvailabilityTest covers RowKind x skipped x adjusted x whatIf x canUndo x forecast failure, with the hint key checked | HotkeyTableTest: no chord conflict inside a scope; text() equals the spec §7 block byte for byte; Alt+Shift alternates are present for desktop too | StatusModelTest covers every segment variant, including web store names and the disabled recorder | The catalogue test reports no duplicate or unused keys in the owned files
---- [core-views] paths: core/src/main/java/ru/cashprediction/core/ui/view/summary/**, core/src/main/java/ru/cashprediction/core/ui/view/table/**, core/src/main/java/ru/cashprediction/core/ui/view/popup/**, core/src/main/resources/ru/cashprediction/core/ui/text/summary.properties, core/src/main/resources/ru/cashprediction/core/ui/text/table.properties, core/src/main/resources/ru/cashprediction/core/ui/text/popup.properties, core/src/test/java/ru/cashprediction/core/ui/view/summary/**, core/src/test/java/ru/cashprediction/core/ui/view/table/**, core/src/test/java/ru/cashprediction/core/ui/view/popup/**
Implement SummaryBuilder (9 cards, colours, beyond-horizon variants, goal states, hints); LazyTableModel with the row index, LRU cache, PAST_HEADER collapse, month totals hidden inside a collapsed group, marks order, row and cell styles, one tooltip per row, the three empty states, and scrollToRowId for the first row on or after today; DayCardModel, SparklineModel and CalendarModel builders. All texts come from spec §5.1, §5.2, §5.6 and §8.9.
DONE: SummaryModelTest covers all nine cards with sample plans: negative value, below cushion, beyond horizon, no goal, goal reached and not reached, forecast failure | TableModelTest covers every RowKind text, marks order, strike, italic, past colour, background priority, tooltip lines 1–13, the month total tooltip, the past header in both states, empty states and scroll-to-today | Performance test: a 600-month plan with weekly rules (about 200 000 rows) builds its index in under 300 ms, and the average row fetch is under 1 ms | DayCard, Sparkline and Calendar tests: 8-line cap with «… и ещё N», fewer than 2 points gives «нет данных», weekends coloured expense
---- [core-chart] paths: core/src/main/java/ru/cashprediction/core/ui/view/chart/**, core/src/main/resources/ru/cashprediction/core/ui/text/chart.properties, core/src/test/java/ru/cashprediction/core/ui/view/chart/**
Implement ChartScale (Y ticks on 1/2/5 steps, range including zero, cushion and goal), the X month grid and label cadence, and ChartLayout producing ChartScene primitives in spec §5.3 drawing order: bars band, fill, step balance line sampled to 1500 points, zero, cushion, goal, today, markers per day capped at 400, legend items with tooltips, hit regions for markers and bars, line labels, PlotTransform.dateAt, empty texts. Also provide dayCard and hover from the model.
DONE: ChartScaleTest covers tick steps and labels «150 000» for several ranges, including negative ones | ChartLayoutTest checks primitive order and pixel geometry at 1200x700 and 300x200, margins 80/24/28/32, and X label cadence at 12, 24, 48 and 120 months | Marker aggregation per day (income, expense, mixed) respects filters; over 400 markers gives no markers plus the legend notice; bars band height is min(110, 22 %) | Legend items appear only when their condition holds; tooltip texts match spec; dateAt clamps at the plot edges
---- [core-forms-framework] paths: core/src/main/java/ru/cashprediction/core/ui/form/**, core/src/main/java/ru/cashprediction/core/ui/alert/**, core/src/main/java/ru/cashprediction/core/ui/forms/simple/**, core/src/main/java/ru/cashprediction/core/io/FolderListing.java, core/src/main/resources/ru/cashprediction/core/ui/text/forms-misc.properties, core/src/main/resources/ru/cashprediction/core/ui/text/alerts.properties, core/src/main/resources/ru/cashprediction/core/ui/text/buttons.properties, core/src/main/resources/ru/cashprediction/core/ui/text/restore.properties, core/src/test/java/ru/cashprediction/core/ui/form/**, core/src/test/java/ru/cashprediction/core/ui/alert/**, core/src/test/java/ru/cashprediction/core/ui/forms/simple/**, core/src/test/java/ru/cashprediction/core/io/FolderListingTest.java
Implement FormSession (StatefulWindow capture and apply, register on shown, unregister on close, touch on change and bounds, clientRev echo rule), FieldCodec (canonical, display, acceptLegacy), FieldChecks, AlertSession and AlertCatalog with every alert of spec §6 and §6A, plus the simple forms: TextInputForms (rename, reconcile, customMonths, customCurrency), ChoiceForms currency, OpenPlanForm, ConfirmForms (deleteRule, deleteOneTime, actualize, applyWhatIf, clearSnapshots), CsvExportForm and FileBrowserForm over FolderListing (moved from web FolderBrowserApi).
DONE: FieldCodecTest accepts «80000», «80 000,5», «80000.50», «-1 200», ISO and dd.MM.yyyy dates, ДД.ММ and ММ-ДД month-day; invalid raw text is kept; display reformatting matches spec | FormSessionTest: capture then JSON, XML and Markdown codecs then applyState gives identical fields, page and bounds; register and unregister are called exactly once | One test per simple form: defaults, every error and warning text, button enabled states, outcomes, and the spec button order | AlertCatalogTest: every alert window title, header, content, details expanded or collapsed flag, buttons and default button equal spec text | FileBrowserFormTest on temporary folders: drives, up, CashMemory, hidden service files, not-found and access errors, extension appended in save mode
---- [core-forms-plan] paths: core/src/main/java/ru/cashprediction/core/ui/forms/plan/**, core/src/main/resources/ru/cashprediction/core/ui/text/forms-plan.properties, core/src/test/java/ru/cashprediction/core/ui/forms/plan/**
Implement NewPlanWizardForm (3 pages, «Открыть пример» LEFT button, step errors «✖ Шаг N:»), PlanSettingsForm (read-only name when the plan has a file), HorizonFields and GoalCalculatorForm (result lines, catches forecast exceptions, reevaluates on document change). Port the logic from the FX dialogs and apply the spec deltas. Code only against the Stage 0 FormLogic and FormSession contracts.
DONE: One test per form covers every field default, visibility and enablement rule, every error and warning text of spec §6.1, §6.2 and §6.6, and button enablement per page | The wizard outcome builds the expected Plan (rules only for filled amounts, weekend policy NONE); the settings outcome produces a single undo step | GoalCalculatorForm with a throwing forecast shows the problem line «✖ Прогноз не рассчитан: …» and never throws
---- [core-forms-ops] paths: core/src/main/java/ru/cashprediction/core/ui/forms/ops/**, core/src/main/resources/ru/cashprediction/core/ui/text/forms-ops.properties, core/src/test/java/ru/cashprediction/core/ui/forms/ops/**
Implement RuleEditorForm (recurrence kinds, preview of 6 dates with shift and adjustment suffixes, preview context menu and OpenChild adjustment), OneTimeForm (default kind Расход), AdjustmentForm (vertical actions, LEFT reset button, missing rule variant) and QuickEditForm (CHANGE_AMOUNT, REPLACE when already moved, removal when equal to the rule).
DONE: One test per form covers every error and warning of spec §6.3–§6.5 and §5.6.1, including the plural «N корректировок перестанут совпадать» | RuleEditorForm preview texts and placeholders; monthDay accepts ДД.ММ and ММ-ДД and stores ММ-ДД; the adjust-selected-date button is enabled only in edit mode with a selection | QuickEditForm outcomes cover the three adjustment cases and keep the note
---- [core-legacy-fixtures] paths: core/src/test/resources/session/legacy/**, core/src/test/java/ru/cashprediction/core/ui/legacy/**
Record crash-recovery fixtures from the current legacy clients built on the S0 core. Run the FX and Swing self-test scripts and the web API with an isolated --home and -Dcashprediction.registry.node=ru/cashprediction/selftest/<uuid>. Open each of the 11 WindowTypes and every TEXT_INPUT, CHOICE and ALERT purpose with typical and invalid values, halt, and copy the registry JSON, XML and web-session.md. Write LegacySnapshotCompatTest: each fixture decodes; FieldCodec.acceptLegacy yields canonical values; FormSession.applyState of the new form shows the expected display texts. Delete the test registry nodes afterwards.
DONE: Fixtures exist for every WindowType and purpose supported by each legacy client (fx, swing, web), each with a README-style comment in the test class naming the legacy source | The stamps of the real HKCU session/fx and session/swing nodes are unchanged before and after recording; no selftest node is left behind | LegacySnapshotCompatTest is green on the S1 integration branch after the three form tasks merge
######## S2 Core controller, generic restore, JSON protocol, scenarios and goldens
---- [core-app-file] paths: core/src/main/java/ru/cashprediction/core/app/AppController.java, core/src/main/java/ru/cashprediction/core/app/AppState.java, core/src/main/java/ru/cashprediction/core/app/StatusMessages.java, core/src/main/java/ru/cashprediction/core/app/OpenWindows.java, core/src/main/java/ru/cashprediction/core/app/flow/FileFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/ExitFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/AutosaveService.java, core/src/main/java/ru/cashprediction/core/app/flow/SettingsKeeper.java, core/src/main/java/ru/cashprediction/core/app/flow/ExternalChangeGuard.java, core/src/main/java/ru/cashprediction/core/app/flow/FileChooserService.java, core/src/test/java/ru/cashprediction/core/app/fake/**, core/src/test/java/ru/cashprediction/core/app/file/**
Implement the AppController shell: UiIntents dispatch through CommandAvailability, the modality rule, part-hash rendering, status messages with 10 s expiry and menu-hover hints, and OpenWindows (modal chain, single goal calculator). Implement FileFlow (new, open, open file, sample, recent, save, overwrite, save as, rename, CSV, PNG through port.renderChartPng, CashMemory folder with browse folder), ExternalChangeGuard, AutosaveService, SettingsKeeper, FileChooserService (native vs server browser, extension appended, confirm.replaceFile unless nativeReplacePrompt) and ExitFlow.
DONE: File flow tests with FakeUiPort scripted answers: confirmDiscard before each of its 6 entry points (Save, Don't save, Cancel, failed save aborts) | Save conflict tests: overwrite on first save, external change with overwrite, reload and cancel, one prompt at a time during autosave, autosave skipped status | Rename with file collision, recent list update, missing recent file error removes the entry; exit order checked (snapshot, confirm, stop timers, settings, shutdownClean, exit CLEAN) | Every UiIntents entry asserts the UI thread in tests; modal-open ignores main commands
---- [core-app-edit] paths: core/src/main/java/ru/cashprediction/core/app/flow/EditFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/ViewFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/ToolsFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/HelpFlow.java, core/src/test/java/ru/cashprediction/core/app/edit/**
Implement EditFlow (rule, one-time and adjustment create, edit and delete; skip and reset with statuses; quick amount; disable rule; plan settings; actualize; reconcile; custom months), ViewFlow (mode, flags, period, slider commit, filter, focus filter, past group, show in table with auto-expand, reveal, scroll-to-today) and ToolsFlow (goal calculator single instance with toFront, what-if toggles, spinner with 600 ms delay, apply and reset, validate, cleanup, currency and custom currency), plus HelpFlow (about, hotkeys, format). Undo texts and statuses follow spec §8.2 and §8.4.
DONE: Flow tests for every command of spec §3 and §5 context menus, checking the resulting plan, the undo text and the status message | editRow dispatch by kind; WHAT_IF gives the hint; START opens settings; disabled command via hotkey gives the hint and no action | Show-in-table expands the past group and selects the first row on or after the date, or shows the noRowAfter status | Goal calculator survives a failing forecast; the what-if apply confirmation text parts are correct
---- [core-app-session] paths: core/src/main/java/ru/cashprediction/core/app/flow/StartupFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/RecoveryFlow.java, core/src/main/java/ru/cashprediction/core/app/flow/SessionBridge.java, core/src/main/java/ru/cashprediction/core/app/flow/CoreWindowFactory.java, core/src/main/java/ru/cashprediction/core/app/flow/FormCatalog.java, core/src/main/java/ru/cashprediction/core/app/flow/SessionStores.java, core/src/test/java/ru/cashprediction/core/app/session/**
Implement StartupFlow (clean start with last plan, first plan or wizard; crashed with the recovery alert before main, RestoreCoordinator and report; already running; load diagnostics; RECORDER_NOT_STARTED save loop with re-ask on chooser cancel or write error), RecoveryFlow (snapshot now, show last with both stores and the default store first, clear, simulate halt and exception), the uncaught handler (saveNow, fatal alert, HALT 2, second error to stderr), SessionBridge (captureMain and applyMain including the extra filters keys and whatIfExtra; RestoreTarget), CoreWindowFactory, FormCatalog and SessionStores. Do not change RestoreCoordinator, CrashDetector, SessionRecorder or the stores.
DONE: GenericRestoreTest: capture, then XML, in-memory registry and Markdown stores, then a new controller restores windows, owners, pages, fields, bounds, view, period, flags, filter, selection, pastExpanded and what-if identically | StartupFlowTest covers every branch: registry, XML, none, unreadable, empty, already running, and RECORDER_NOT_STARTED including chooser cancel and write failure loops | Restoring the S1 legacy fixtures through CoreWindowFactory succeeds; unknown purpose and missing target give the exact restore.warn texts | The existing RestoreCoordinatorTest, CrashDetectorTest and SessionRecorderTest pass unmodified
---- [core-protocol-dump] paths: core/src/main/java/ru/cashprediction/core/ui/json/**, core/src/main/java/ru/cashprediction/core/ui/dump/**, core/src/main/java/ru/cashprediction/core/ui/selftest/**, core/src/main/resources/ru/cashprediction/core/ui/scenarios/**, core/src/test/resources/ui-golden/**, core/src/test/resources/ui-json/**, core/src/test/java/ru/cashprediction/core/ui/json/**, core/src/test/java/ru/cashprediction/core/ui/dump/**, core/src/test/java/ru/cashprediction/core/ui/selftest/**, docs/ui-protocol.md
Implement UiJson (records to JSON and intents from JSON), the UiDump schema, DumpNormalizer, DumpDiff and AllowedDiffs (seeded exactly from spec §10), the self-test script parser (ported from Swing SelfTestTokenizer and FX FxSelfTest commands, plus the new commands of design §6.2), SelfTestRunner, UiDriver and ModelUiDriver, and the 18 scenario files. Produce ui-json fixtures (bootstrap, every effect, every intent) for web development. Generate the goldens after the other S2 tasks merge and review them against docs/ui-spec.md section by section.
DONE: UiJsonTest round-trips every intent, query, effect and model record; the fixtures in core/src/test/resources/ui-json validate | DumpDiffTest: an allowed diff passes, an unallowed diff fails with its JSON pointer, and an allowance that matches nothing fails | All 18 scenarios run headless through ModelUiDriver; goldens are committed; UiGoldenTest is green in the default build | After a full scenario run the catalogue test reports zero unused keys across all properties files | docs/ui-protocol.md matches UiJson output for the fixtures
---- [test-infra] paths: ui-parity/**
Build the parity harness on top of the S0 launcher: per-scenario dump collection per client (selected by -Dparity.clients=fx,swing,web), normalisation and comparison through core DumpDiff against the goldens and pairwise, an HTML report with pointer diffs, plus scaffolding for HotkeyParityTest, ClassUsageTest and VisualParityTest (region colour and position checks from dump boxes).
DONE: ParityPipelineIT runs with a fake client main that copies golden dumps (one mutated on purpose) and shows the expected failure in target/parity/report.html | Region check utilities are unit-tested on synthetic PNGs (ΔE threshold, ±4 px) | Harness modules depend only on core, JDK and JUnit
######## S3 Renderers: JavaFX, Swing, web server, web browser (core API frozen)
---- [ui-fx] paths: ui-fx/pom.xml, ui-fx/src/main/java/module-info.java, ui-fx/src/main/java/ru/cashprediction/fx/FxMain.java, ui-fx/src/main/java/ru/cashprediction/fx/ui/**, ui-fx/src/main/resources/ru/cashprediction/fx/ui/**, ui-fx/src/test/**
Build the generic JavaFX renderer of design §4.1 in the new package fx.ui, leaving legacy code untouched and selected by --ui legacy. Start with the day-1 spikes of design §8 (button order, menu item styleable node, exactly-once hotkeys, data-URI stylesheet under jlink, Canvas cost). Build FxUiPort, the main window parts, the forms, alerts, popups, choosers, FxKeyBridge, FxCrashHooks, FxUiDriver, FxUiDumper and FxClassUsageProbe. Use the 23 required classes exactly as in the design mapping table, each with its // JavaFX: X → Swing: Y → Web: Z comment. Adapted copies of the legacy hosts and popups go into fx.ui.
DONE: mvn -B install is green; NoCyrillicLiteralsTest and MappingCommentTest pass for fx.ui | In mvn -Pui-tests with -Dparity.clients=fx, all 18 scenarios equal the goldens with only spec §10 allowances | HotkeyParityTest fx: every binding fires exactly once; disabled commands show the status hint | ClassUsageTest fx: all 23 classes are instantiated during the scenarios | Screenshots of s02, s03, s05 and s08 are produced in target/parity/visual; the legacy UI still starts with --ui legacy
---- [ui-swing] paths: ui-swing/pom.xml, ui-swing/src/main/java/module-info.java, ui-swing/src/main/java/ru/cashprediction/swing/SwingMain.java, ui-swing/src/main/java/ru/cashprediction/swing/ui/**, ui-swing/src/main/resources/ru/cashprediction/swing/ui/**, ui-swing/src/test/**
Build the generic Swing renderer of design §4.2 in the new package swing.ui, with legacy untouched behind --ui legacy. Build SwingLook (Metal, token colours, composite fonts, Russian JFileChooser and OptionPane keys), SwingUiPort, the menus with slider and spinner items, the toolbar, summary with WrapLayout, the JTable adapter, the Graphics2D chart painter (also used for PNG), status bar, calendar, the forms in spec button order, alerts, popups, SwingKeyBridge, SwingUiDriver and SwingUiDumper. Each analog carries its mapping comment.
DONE: mvn -B install is green including the headless SwingMenusTest, SwingTableAdapterTest, SwingLookGlyphCoverageTest and NoCyrillicLiteralsTest | In mvn -Pui-tests with -Dparity.clients=swing, all 18 scenarios equal the goldens with only spec §10 allowances | HotkeyParityTest swing passes; no English text appears in chooser or option pane dumps | Screenshots of s02, s03, s05 and s08 are produced; the legacy UI still starts with --ui legacy
---- [web-server] paths: web/pom.xml, web/src/main/java/module-info.java, web/src/main/java/ru/cashprediction/web/WebMain.java, web/src/main/java/ru/cashprediction/web/WebServer.java, web/src/main/java/ru/cashprediction/web/StaticHandler.java, web/src/main/java/ru/cashprediction/web/ui/**, web/src/test/java/ru/cashprediction/web/ui/**
Build the server side of design §4.3 and §5: WebUiPort with EffectLog, ControllerThread, UiApi (bootstrap, events long-poll, intent, query), InteractionRegistry with first-answer-wins, WebChartPng (headless Java2D), WebSelfTestBridge behind --test-api, the /app/tokens.css route and pre-bootstrap texts in the served html. Serve /app.html when --ui core; legacy / stays until S4.
DONE: UiApiTest over real HTTP with a temp CashMemory and an in-memory registry: bootstrap; intent returns inline effects; long-poll ordering; resync after more than 1000 effects; two tabs answering one alert (first wins, close broadcast); reload returns open forms with typed values; rows paging with a stale rev; test API off by default; token and Host checks | WebChartPngTest renders 1200x700 headless and samples token colours at known primitive positions | The legacy web API tests are still green
---- [web-js] paths: web/src/main/resources/web/app/**, web/src/main/resources/web/app.html, web/src/test/java/ru/cashprediction/web/js/**
Build the browser renderer of design §4.4 in vanilla ES modules against docs/ui-protocol.md and the core ui-json fixtures: transport, keys, menu, toolbar, summary, virtual table, chart as SVG from ChartScene, status bar, form dialogs with the closed widget set and own calendar, alerts, popups and tooltips, offline screens, the test driver and the DOM dumper. No Cyrillic literals, no emoji, no CDN.
DONE: NoCyrillicLiteralsInJsTest is green | In mvn -Pui-tests with -Dparity.clients=web (Edge headless), all 18 scenarios equal the goldens with only spec §10 allowances and there are no console errors | HotkeyParityTest web passes, including Alt+Shift alternates and physical keys in the Russian layout | A virtual table with 200 000 rows renders a page in under 50 ms (Runtime.evaluate timing); at 400 px width there is no horizontal page scroll
---- [core-maintainer] paths: core/**, docs/ui-spec.md
Fix core model and flow defects reported by renderer agents as failing scenario steps, without public signature changes (any unavoidable contract change is broadcast and versioned in docs/ui-protocol.md). Update goldens and docs/ui-spec.md in the same commit when the spec was ambiguous.
DONE: mvn -B install is green after every fix | Every fix has a unit or flow test reproducing the reported step | The golden diff for each spec clarification is reviewed and noted in the docs/ui-spec.md change list
######## S4 Three-way parity, crash-recovery e2e, default flip and legacy deletion
---- [test-infra] paths: ui-parity/**, .github/workflows/**
Finish ParityTest across all clients, HotkeyParityTest, ClassUsageTest, VisualParityTest with report.html, CrashRestoreE2ETest (fx and swing with registry, xml and none; web kill and restart; nested and modeless windows), AlreadyRunningE2ETest and UnsavedPlanRestoreE2ETest. Add the ui-tests CI job on windows-latest (blocking) and e2e to the release workflow.
DONE: mvn -Pui-tests verify is green for fx, swing and web on all 18 scenarios with only §10 allowances; pairwise boxes are within ±4 px | The CrashRestoreE2ETest matrix is green: restored dumps equal before-crash dumps for windows, owners, pages, fields, bounds, view, selection, flags and what-if; none clears both stores; a corrupted registry does not affect the XML restore | Every run leaves no process and no selftest registry node behind; the real session nodes are untouched | CI workflow files are updated and pass on a pull request
---- [ui-fx] paths: ui-fx/**
Fix remaining fx-only parity diffs, make --ui core the default, then delete the legacy JavaFX code listed in design §7 and remove the switch from FxMain.
DONE: The legacy classes of design §7 are gone; mvn -B install is green; the ui-fx main source is about 6k lines or less | Parity and e2e for fx stay green after deletion; ClassUsageTest is still 23/23
---- [ui-swing] paths: ui-swing/**
Fix remaining swing-only parity diffs, flip the default to core, delete the legacy Swing code listed in design §7.
DONE: The legacy classes are gone; mvn -B install is green; the ui-swing main source is about 6.5k lines or less | Parity and e2e for swing stay green after deletion
---- [web] paths: web/**
Fix remaining web-only diffs, delete the legacy web Java APIs and JS listed in design §7, rename app.html to index.html, and port any still-relevant legacy API tests into UiApiTest.
DONE: No legacy route or JS file remains; mvn -B install is green | Parity and e2e for web stay green; token and Host checks are still tested
---- [core-maintainer] paths: core/**, docs/**
Remove the --ui switch from LaunchOptions once all three clients have flipped. Delete core helpers used only by legacy clients (keep PlanJson if still needed by tests). Resolve cross-client findings in core with golden updates.
DONE: mvn -B install is green; no reference to legacy UI mode remains | docs/ui-architecture.md reflects the final package list
######## S5 First-user UX pass per client and packaging
---- [core-ux] paths: core/**, docs/ui-spec.md
Review the whole flow as a first-time user without changing public APIs: text audit (typos, consistent «сеанс», «ё», actionable errors), sensible defaults, empty states, the first-run wizard path, large-plan performance (open a 600-month weekly plan in under 2 s; filter keystroke to render in under 100 ms), no ISO dates or emoji anywhere in dumps.
DONE: A checklist of reviewed items is recorded in the docs/ui-spec.md change list, with every text change reflected in goldens | Performance tests for the budgets are added and green | mvn -B install is green
---- [ui-fx] paths: ui-fx/**
JavaFX UX pass: screenshots at 100, 125 and 150 percent scaling; a keyboard-only walkthrough of s01, s05, s06 and s15; visible focus; no clipped labels or ellipsis in dialogs at the minimum widths; the minimum window 900x600; dialogs fit on a 1366x768 screen; the native chooser path works.
DONE: No clipping or overlap findings remain at 100, 125 and 150 percent (screenshots attached to target/parity/visual) | The keyboard-only walkthrough completes each scenario without the mouse | Parity stays green
---- [ui-swing] paths: ui-swing/**
Swing UX pass with the same checklist as JavaFX, plus glyph rendering, JFileChooser Russian texts and Metal theme polish (focus rings, table header, menus).
DONE: No clipping or glyph-box findings at 100, 125 and 150 percent | The keyboard-only walkthrough completes; parity stays green
---- [web] paths: web/**
Web UX pass in Edge (and Chrome if installed): 400 px and 1920 px widths, reload in the middle of a dialog, server stop and crash screens, clipboard copy, tooltips, the file browser window, two tabs, back and forward navigation.
DONE: No layout or horizontal-scroll findings; reload restores open windows; offline screens appear within 30 s | Parity stays green
---- [dist] paths: dist/**, .github/scripts/**, README.md, BUILD.md, CHANGELOG.md
Pass the launcher arguments through the three jpackage launchers; add Test-Portable.ps1 -Parity (runs each exe with --home, --registry-node and --selftest s02-sample-table, compares the dump with the golden, and does one crash/restore per exe with registry node cleanup). Update BUILD.md and README.md test commands and write the CHANGELOG entry listing the user-visible changes from the old clients.
DONE: mvn -B -Pdist package builds the portable folder with 3 exe | PortableExeE2ETest and Test-Portable.ps1 -Parity pass; the first run creates only CashMemory next to the programme | CHANGELOG lists every user-visible behaviour change (for example the Swing editable plan name becoming read-only, and the web import and download removal)
######## S6 Final verification and sign-off
---- [release-verifier] paths: docs/parity-signoff.md, docs/release-checklist.md
Run the full matrix on a clean checkout: default install, -Pui-tests, -Pe2e and -Pdist with e2e. Walk the visual report for all 18 scenarios side by side against the spec. Check the 23-class census and mapping comments. Do one manual crash and restore per exe from both registry and XML, and one from the web server. Record results. Defects go back to the owning agent; this task edits no code.
DONE: Every profile is green on a clean checkout, with runtimes recorded | docs/parity-signoff.md records OK or a linked defect for each scenario and for each spec section §1–§10 | The manual crash and restore passes for FX (registry and XML), Swing (registry and XML) and Web | No selftest registry nodes and no stray processes remain; the working tree is clean
